const styles = {
  image: (width: number) => ({
    marginLeft: "5px",
    width: width ? `${width}px` : "25px",
    borderRadius: "50%",
  }),
  iconExtra: (width: number) => ({
    width: width + 10 + "px",
  }),
}

export default styles;